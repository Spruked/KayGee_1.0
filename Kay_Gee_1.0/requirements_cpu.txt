## Kay_Gee CPU-only requirements
# Use this file on machines without NVIDIA GPUs / CUDA. It omits CUDA-specific builds
# and pulls CPU-only PyTorch wheels from the official PyTorch CPU index.

# Core ML and Scientific Computing
scikit-learn>=1.3.0
numpy>=1.24.0
scipy>=1.10.0

# Graph and Network Analysis
networkx>=3.1

# Database
sqlalchemy>=2.0.0

# Cryptography for Handshake Protocol
cryptography>=41.0.0
pynacl>=1.5.0

# Prolog Integration
pyswip>=0.2.10

# Natural Language Processing (Classical, Non-Neural)
nltk>=3.8.0

# Similarity Search
annoy>=1.17.0

# Audio Processing and Speech (non-PyTorch parts)
TTS>=0.22.0
faster-whisper>=0.10.0
librosa>=0.9.2
soundfile>=0.12.1
pydub>=0.25.1

# Testing
pytest>=7.4.0
pytest-cov>=4.1.0

# Development
black>=23.0.0
pylint>=2.17.0
mypy>=1.4.0

# Utilities
python-dotenv>=1.0.0
pyyaml>=6.0.0

# Logging
structlog>=23.1.0

# --- CPU-only PyTorch wheels ---
# The following two lines instruct pip to look for CPU-only PyTorch wheels
# hosted by PyTorch. Keep them at the bottom of this file.
-f https://download.pytorch.org/whl/cpu/torch_stable.html
# PyTorch CPU wheel index
-f https://download.pytorch.org/whl/cpu/torch_stable.html
# Pin matching CPU builds that are known compatible on Windows
# Use torch 2.0.0 + torchaudio 2.0.0 (matching dependencies)
f https://download.pytorch.org/whl/cpu/torch_stable.html
# Pin matching CPU builds that are known compatible on Windows
# Use torch 2.1.x + torchaudio 2.1.x to satisfy TTS >=2.1
## CPU-only PyTorch wheel index
-f https://download.pytorch.org/whl/cpu/torch_stable.html

# Pin matching CPU-only builds that satisfy TTS (torch>=2.1)
torch==2.1.2+cpu
torchaudio==2.1.2+cpu

# Notes:
# - On Windows, using the above +cpu wheel spec together with the -f
#   index ensures pip will install CPU-only PyTorch/torchaudio.
# - If you prefer a different torch version, change the pinned version
#   but keep the +cpu suffix and the -f index line.
